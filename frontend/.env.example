# .env.local

# NextAuth.js secret (you can generate one using openssl or any secret generator)
NEXTAUTH_SECRET=your_nextauth_secret
NEXTAUTH_URL=http://localhost:3000

# GitHub OAuth credentials
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret

# Google OAuth credentials (commented out, uncomment if needed)
# GOOGLE_CLIENT_ID=your_google_client_id
# GOOGLE_CLIENT_SECRET=your_google_client_secret

# Public API URL for Strapi
NEXT_PUBLIC_API_URL=http://localhost:1337

# Strapi GraphQL endpoint
STRAPI_GRAPHQL_URL=$NEXT_PUBLIC_API_URL/graphql

# Strapi API token, generated using the Strapi admin panel
# it is better to work with a user with limited permissions in production
STRAPI_API_TOKEN=your_strapi_api_token
